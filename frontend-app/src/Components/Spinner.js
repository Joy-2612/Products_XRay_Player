// Spinner.js
import React from "react";
import "../styles/Spinner.css"; // Import the CSS file for spinner styling

const Spinner = () => {
  return <div className="spinner"></div>;
};

export default Spinner;
